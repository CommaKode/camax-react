import React from 'react';
import AboutUs from './AboutUs';

export default {
  title: 'PAGES/Home/About Us',
  component: AboutUs,
};

const Template = (args) => <AboutUs {...args} />;

export const Primary = Template.bind({});
