import HamButton from './HamButton';

export default {
  title: 'NavBar/HamButton',
  component: HamButton,
};

const Template = (args) => <HamButton {...args} />;

export const Button = Template.bind({});
