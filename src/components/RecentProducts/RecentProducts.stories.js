import RecentProducts from './RecentProducts';

export default {
  title: 'Pages/Home/RecentProducts',
  component: RecentProducts,
};

const Template = (args) => <RecentProducts {...args} />;

export const Phone = Template.bind({});
