import React from 'react';
import Cards from './Cards';

export default {
  title: 'PAGES/Home/Cards',
  component: Cards,
};

const Template = (args) => <Cards {...args} />;

export const Primary = Template.bind({});
