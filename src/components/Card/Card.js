import React from 'react'
import PropTypes from 'prop-types'

const Card = props => {
    const {title , }
    return (
        <div>
            
        </div>
    )
}

// Card.propTypes = {

// }

export default Card
